{EventEmitter} = require 'events'

class Workspace extends EventEmitter
  constructor: (@app, @path) ->
    @documents = []

  set_path: (@path) ->
    # Let any subscribers know that this value has changed.
    @.emit 'kitana.workspace.path_changed'

exports.Workspace = Workspace
